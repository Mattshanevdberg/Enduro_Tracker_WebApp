{ "global": {
    "database_url": "postgresql+psycopg://user:pass@host:5432/dbname", # Compose DATABASE_URL should remain the runtime source of truth
    "debug_mode": true,
    "api_host": "0.0.0.0",
    "api_port": 5000,
    "allowed_origins": ["*"],
    "timezone": "Africa/Johannesburg", #"UTC", # this is the default timezone for the application
    "category": ["Professional", "Open", "Junior"]
  },
  "example": "example_text"
}
